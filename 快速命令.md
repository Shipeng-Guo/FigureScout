# FigureScout 快速命令卡片

> 最常用的命令，一页搞定！

---

## 🚀 启动应用

```bash
cd ~/wsl_projects/FigureScout
bash start.sh
```

**访问**: http://localhost:3000

---

## 🛑 关闭应用

```bash
cd ~/wsl_projects/FigureScout
bash stop.sh
```

**或按**: `Ctrl + C`（如果前台运行）

---

## 📊 检查状态

```bash
cd ~/wsl_projects/FigureScout
bash status.sh
```

---

## 🔧 常用命令

```bash
# 查看后端日志
tail -f backend/backend.log

# 查看前端日志
tail -f frontend/frontend.log

# 强制重启
bash stop.sh && sleep 2 && bash start.sh

# 清除缓存重启
pkill -f vite
cd frontend && rm -rf node_modules/.vite
npm run dev
```

---

## 🆘 紧急处理

```bash
# 强制关闭所有进程
pkill -9 -f "python app.py"
pkill -9 -f "node.*vite"

# 清理端口
fuser -k 5000/tcp
fuser -k 3000/tcp

# 重新启动
bash start.sh
```

---

## 📝 版本发布

```bash
# 1. 更新版本号
echo "1.6.0" > VERSION

# 2. 同步版本
bash sync_version.sh

# 3. 提交推送
git add .
git commit -m "chore: release v1.6.0"
git tag v1.6.0
git push origin main
git push origin v1.6.0
```

---

**提示**: 打印此文档放在桌面，需要时快速查看！

